<%= render 'shared/navbar' %>

<div class="container mx-auto mt-16 p-4 bg-white rounded-lg">
    <p class="text-lg text-gray-700 mb-4">
      <span class="font-medium">Email:</span> <%= @user.email_address %>
    </p>
  <%= link_to 'Удалить аккаунт', delete_user_path, data: { turbo_frame: "modal" }, confirm: "Are you sure?", class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" %>
</div>